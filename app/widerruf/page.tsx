import { LegalDocument } from '@/components/LegalDocument'

export const metadata = { title: 'Widerruf | Nebula Noir' }

export default function Page() {
  return <LegalDocument section="widerruf" />
}
