import { LegalDocument } from '@/components/LegalDocument'

export const metadata = { title: 'Custom Orders | Nebula Noir' }

export default function Page() {
  return <LegalDocument section="customOrders" />
}
